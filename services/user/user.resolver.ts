import { ResolverContext } from '../../apollo/types'
import { Resolvers } from '../schema.graphqls'

const resolver: Resolvers<ResolverContext> = {
  Query: {
    //
  },
  Mutation: {
    //
  },
}

export default resolver
